<h1>List of Students</h1>
<table id='boot' class='table table-hover table-striped filterable'>
	<thead>
		<tr>
			<th>Last Name</th>
			<th>First Name</th>
			<th>Email</th>
			<th>Balance</th>
			<th>Dance Credit</th>
		</tr>
	</thead>
	<tbody>
		<% @students.each do |student| %>
		<tr<%=student.in_debt? ? " class=unpaid" : "" %>>
			<td><a href=<%= "/students/#{student.id}" %>><%= student.lastname %></td>
			<td><a href=<%= "/students/#{student.id}"%>><%= student.firstname %></td>
			<td><%= student.email %></td>
			<td><a href=<%="/students/#{student.id}/transactions"%>><%= number_to_currency(student.balance) %></a></td>
			<td><%= student.credit %></td>
		</tr>
		<% end %>
	</tbody>
</table>