<div class='span11'>
	<h1>Attendance sheet for <%= @classrecord.created_at.to_time.strftime("%m/%d/%Y") %> <span class="badge"><%=number_to_currency(@classrecord.cost)%></span></h1><small><%= link_to "View transactions for this class", transactions_classrecord_path(:id=>@classrecord.id)%></small></h1>
	<%= render 'form' %>
</div>