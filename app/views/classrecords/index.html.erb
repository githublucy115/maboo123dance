<h1>Class history</h1>

<table id="boot" class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Date</th>
      <th>Cost</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @classrecords.each do |classrecord| %>
      <tr>
        <td><%= classrecord.date %></td>
        <td><%= link_to number_to_currency(classrecord.cost), expenses_classrecord_path(:id=>classrecord.id) %></td>
        <td><%= link_to 'View students in this class', classrecord %></td>
      </tr>
    <% end %>
  </tbody>
</table>